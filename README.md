# api_scheduler
 
API Scheduler can schedule tasks on server farms with multiple nodes using a priority queuing system. 

ReactJS and Django are used for the front-end and backend respectively.  SQLite is used for persistent data storage.
